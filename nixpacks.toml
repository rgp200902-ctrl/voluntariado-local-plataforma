[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.build]
cmds = ["pip install -r requirements.txt", "python manage.py collectstatic --noinput", "python manage.py migrate", "python manage.py seed_db"]

[start]
cmd = "gunicorn config.wsgi --log-file -"
