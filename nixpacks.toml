[phases.setup]
nixPkgs = ["python311"]

[phases.build]
cmds = ["python manage.py collectstatic --noinput", "python manage.py migrate", "python manage.py seed_db"]

[start]
cmd = "gunicorn config.wsgi --bind 0.0.0.0:$PORT --log-file -"